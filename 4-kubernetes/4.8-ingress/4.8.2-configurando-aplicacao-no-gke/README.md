# Configurando aplicação no GKE.

- Comandos usados em aula:

- Aplicando todos yaml do diretório:
```bash
kubectl aplly -f /diretório
```

- Listando Pods após aplicar todos yamls:
```bash
kubectl get po
```

- Visualizando services na cloud da Google:
```bash
kubectl get svc
```
